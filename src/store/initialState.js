import { AllPosts, Categories, Sections, Sidebar } from "../components/blog/constants";

const posts = AllPosts;
const categories = Categories;
const sections = Sections;
const sidebar = Sidebar;

const store = {
  posts,
  categories,
  sections,
  sidebar,
};

export default store;
